using UnityEngine;

public class NoteObject : MonoBehaviour
{
    public bool isHoldNote = false; // Kalau dicentang, berarti ini Hold Note
    public bool sedangDitekan = false; // Status apakah pemain lagi nahan tombol
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
